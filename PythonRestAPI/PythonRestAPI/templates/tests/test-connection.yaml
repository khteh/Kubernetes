apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "PythonRestAPI.fullname" . }}-test-connection"
  labels:
    {{- include "PythonRestAPI.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "PythonRestAPI.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
