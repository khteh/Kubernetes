apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "common-config.fullname" . }}-test-connection"
  labels:
    {{- include "common-config.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "common-config.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
